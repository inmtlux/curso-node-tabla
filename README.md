# Notas
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -n, --numero   Es la base de la tabla de multiplicar       [number] [required]
  -m, --multi    muestra la tabla de multiplicar      [boolean] [default: false]
  -s, --sumar    muestra la tabla de sumar            [boolean] [default: false]
  -h, --hasta    hasta donde se hara la tabla                [number] [required]

```